# Examples

**Example 1:**

Input: s = "abcd", t = "abcde"
Output: "e"

**Example 2:**

Input: s = "", t = "y"
Output: "y"

# Constraints

- `0 <= s.length <= 1000`
- `t.length == s.length + 1`
- `s` and `t` consist of lowercase English letters.
